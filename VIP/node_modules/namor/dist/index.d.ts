import generate from "./generate";
import validate from "./validate";
interface DataManly {
    adjectives: string[];
    nouns: string[];
    verbs: string[];
}
interface Data {
    adjectives: string[];
    nouns: string[];
    verbs: string[];
    reserved: string[];
    manly: DataManly;
}
declare const _default: {
    generate: typeof generate;
    validate: typeof validate;
    rawData: Data;
    isValid: (name: string, opts?: any) => boolean;
};
export = _default;
